/**
 * @fileoverview MSW (Mock Service Worker) handlers — mock API responses
 * for testing without a real backend.
 */

export const handlers = [
  // Define API mocks here using MSW if installed
  // Example:
  // rest.post('/api/v1/auth/login', (req, res, ctx) => {
  //   return res(ctx.json({ success: true, data: { accessToken: 'mockToken' } }));
  // }),
];
