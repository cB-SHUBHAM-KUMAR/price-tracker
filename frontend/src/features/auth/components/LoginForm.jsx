/**
 * @fileoverview LoginForm component — handles user login.
 * Place feature-specific components inside the feature folder,
 * not in the shared/ui directory.
 */

function LoginForm() {
  return (
    <form className="login-form">
      <h2>Welcome Back</h2>
      {/* Form fields go here */}
    </form>
  );
}

export default LoginForm;
